plugins {
    id 'groovy'
}

subprojects {
    dependencies {
        compile 'org.apache.commons:commons-lang3:3.11'
        compile 'com.google.guava:guava:30.0-jre'
    }
}

project(':shared:shared-bukkit') {
    dependencies {
        compile project(':shared:shared-api')
    }
}